services:
  cozyrss:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - cozyrss-data:/app/data
    environment:
      - SESSION_SECRET=${SESSION_SECRET:-please-change-this-to-a-random-secret}
    restart: unless-stopped

volumes:
  cozyrss-data:
