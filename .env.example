# Session secret for signing cookies (REQUIRED — use a random string of 32+ characters)
SESSION_SECRET=change-me-to-a-random-secret

# Database path (optional — defaults to ./data/cozyrss.db)
# DATABASE_PATH=./data/cozyrss.db

# Port (optional — defaults to 3000)
# PORT=3000
